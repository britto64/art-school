services:
  artschool:
    image: brittinho/artschool:latest
    pull_policy: always
    container_name: artschool
    restart: always
    ports:
      - "3011:3000"
    volumes:
      - /mnt/StorageHD1/artschool/cursos:/courses:ro
      - /mnt/StorageHD1/configs/artschool:/app/data
    environment:
      - COURSES_PATH=/courses
      - DATA_PATH=/app/data
networks: {}
