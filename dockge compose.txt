services:
  artschool:
    image: brittinho/artschool:latest
    pull_policy: always
    container_name: artschool
    restart: always
    ports:
      - "3011:3000"
    volumes:
      # ajuste o caminho da esquerda para onde os cursos ficarem no NAS
      - /mnt/StorageHD1/media/cursos:/courses:ro
      - /mnt/StorageHD1/configs/artschool:/app/data
    environment:
      - COURSES_PATH=/courses
      - DATA_PATH=/app/data
networks: {}
